apiVersion: apps/v1
kind: Deployment
metadata:
  name: nms4cloud-order
  labels:
    app: nms4cloud-order
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nms4cloud-order
  template:
    metadata:
      labels:
        app: nms4cloud-order
    spec:
      containers:
      - name: nms4cloud-order
        image: 192.168.253.128:30083/library/nms4cloud-order:c5b4beb12158-10
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: nacos-client
        env:
        - name: JAVA_TOOL_OPTIONS
          value: -Xms128m -Xmx512m -XX:MaxMetaspaceSize=256m
        ports:
        - name: http
          containerPort: 18139
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 600m
            memory: 1280Mi
---
apiVersion: v1
kind: Service
metadata:
  name: nms4cloud-order
spec:
  selector:
    app: nms4cloud-order
  ports:
  - name: http
    port: 18139
    targetPort: http
