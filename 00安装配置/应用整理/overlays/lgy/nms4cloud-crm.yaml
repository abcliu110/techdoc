apiVersion: apps/v1
kind: Deployment
metadata:
  name: nms4cloud-crm
  labels:
    app: nms4cloud-crm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nms4cloud-crm
  template:
    metadata:
      labels:
        app: nms4cloud-crm
    spec:
      containers:
      - name: nms4cloud-crm
        image: 192.168.253.128:30083/library/nms4cloud-crm:c5b4beb12158-10
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: nacos-client
        env:
        - name: JAVA_TOOL_OPTIONS
          value: -Xms128m -Xmx512m -XX:MaxMetaspaceSize=256m
        ports:
        - name: http
          containerPort: 18138
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 1280Mi
---
apiVersion: v1
kind: Service
metadata:
  name: nms4cloud-crm
spec:
  selector:
    app: nms4cloud-crm
  ports:
  - name: http
    port: 18138
    targetPort: http
