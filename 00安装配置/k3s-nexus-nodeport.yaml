apiVersion: v1
kind: Service
metadata:
  name: nexus-nodeport
  namespace: nexus
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: nexus
    app.kubernetes.io/name: nexus3
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: http
      nodePort: 30081
