# 05 - 证据与未知

> 分析证据清单与未确定项
> 最后更新：2026-08-04

---

## 1. 已验证证据

### 1.1 核心类证据

| 证据 | 文件 | 说明 |
|------|------|------|
| E1 | `PrnJobStatusEnum.java` | 任务状态枚举：PENDING(1), SUCCESS(2), FAILED(3) |
| E2 | `BlockQueueHandler.java` | 队列处理基类，使用 LinkedBlockingQueue |
| E3 | `PrinterWorker.java` | 打印执行器，包含超时(30分钟)、重试(10秒)逻辑 |
| E4 | `PrintJobGenerator.java` | 打印任务生成器，1127 行 |
| E5 | `PosPrnJobController.java` | REST 控制器，含 reprint 接口 |
| E6 | `HandlerFactory.java` | Handler 工厂，支持多种打印机 |

### 1.2 数据模型证据

| 证据 | 文件 | 说明 |
|------|------|------|
| E7 | `PosPrnJob.java` | 打印任务实体 |
| E8 | `PosPrnPrinter.java` | 打印机实体 |
| E9 | `PosPrnQueue.java` | 打印队列实体 |
| E10 | `PosPrnStyle.java` | 打印样式实体 |
| E11 | `PosPrnStyleRow.java` | 样式行实体 |

### 1.3 枚举证据

| 证据 | 文件 | 说明 |
|------|------|------|
| E12 | `PrinterTypeEnum.java` | 打印机类型：DRIVER/NET/COM/USB/LPT/云打印 |
| E13 | `PrinterModelEnum.java` | 打印机型号 |
| E14 | `PrnStyleTypeEnum.java` | 打印样式类型，130+ 种 |
| E15 | `PrnJobStatusEnum.java` | 任务状态枚举 |

---

## 2. 代码片段证据

### 2.1 任务状态枚举

```java
// PrnJobStatusEnum.java
public enum PrnJobStatusEnum {
  PENDING(1, "pending"),   // 待打印
  SUCCESS(2, "success"),   // 打印成功
  FAILED(3, "failed");     // 打印失败
}
```

### 2.2 超时判断

```java
// PrinterWorker.java 约第 180 行
if (Duration.between(jobDate, LocalDateTime.now()).toMinutes() > 30) {
  posPrnJobServicePlus.archiveFailed(
    prnJob.getMid(),
    prnJob.getSid(),
    prnJob.getLid(),
    "Print task timeout"
  );
  return;
}
```

### 2.3 重试延迟

```java
// PrinterWorker.java 约第 196-197 行
} else {
  posPrnJobServicePlus.addPrnCount(jobLid);
  put(dispatchJob, 10 * 1000, 0);
}
```

### 2.4 BlockQueueHandler 队列模式

```java
// BlockQueueHandler.java
@Override
public void run() {
  while (!stopped) {
    try {
      T t = blockingQueue.take();
      runInner(t);
    } catch (Throwable ex) {
      sleep(Duration.ofSeconds(5));
    }
  }
}
```

---

## 3. 未确定项

### 3.1 高优先级未知

| # | 未知项 | 影响 | 验证方法 |
|---|--------|------|----------|
| U1 | 重试次数限制 | 任务可能无限重试 | 搜索 `addPrnCount` 调用，查看是否在达到 N 次后归档 |
| U2 | 打印机 FAULT 判断逻辑 | 故障检测准确性 | 读取 `getPrinterStatus` 实现 |
| U3 | BlockQueueHandler 停止条件 | 服务停止行为 | 搜索 `stopped = true` 设置点 |
| U4 | 启动时队列恢复 | 任务持久性 | 确认 PrinterWorker 启动时是否从 DB 恢复 |

### 3.2 中优先级未知

| # | 未知项 | 影响 | 验证方法 |
|---|--------|------|----------|
| U5 | 云打印密钥管理 | 安全性 | 检查 extra_info 加密策略 |
| U6 | 打印任务清理策略 | 存储增长 | 查找归档任务清理逻辑 |
| U7 | 并发打印处理 | 数据一致性 | 检查是否有锁或事务控制 |
| U8 | 三联打印实现 | 业务完整性 | 确认是否真正实现三联 |

### 3.3 低优先级未知

| # | 未知项 | 影响 | 验证方法 |
|---|--------|------|----------|
| U9 | 打印前预览 | 功能完整性 | 检查是否有预览功能 |
| U10 | 打印日志审计 | 合规性 | 检查审计日志记录 |
| U11 | 前端打印管理 | 用户体验 | 分析 nms4pos-ui 打印相关页面 |

---

## 4. 分析方法说明

### 4.1 信息来源

| 来源 | 说明 |
|------|------|
| 源代码 | 直接阅读 Java 源码 |
| 数据库 | 查询表结构和数据 |
| API 抓包 | 实际请求/响应 |
| 文档 | 项目文档、注释 |

### 4.2 置信度评估

| 等级 | 说明 | 标准 |
|------|------|------|
| 高 | 已直接阅读源码确认 | 代码存在、逻辑清晰 |
| 中 | 间接推断或部分确认 | 部分代码、逻辑推断 |
| 低 | 未直接验证 | 推测或参考类似实现 |

### 4.3 验证状态

| 状态 | 说明 |
|------|------|
| ✅ 已验证 | 源码确认、逻辑清晰 |
| 🔍 待验证 | 有推测、需要进一步确认 |
| ❌ 未验证 | 完全未知、待探索 |

---

## 5. 补充分析建议

### 5.1 高优先级补充分析

1. **重试次数限制**
   ```
   建议搜索：addPrnCount 调用
   期望找到：N 次后触发 archiveFailed
   ```

2. **PrinterWorker 启动恢复**
   ```
   建议搜索：PrinterWorker 构造函数或 @PostConstruct
   期望找到：从 DB 加载 PENDING 任务的逻辑
   ```

3. **FAULT 状态判断**
   ```
   建议读取：getPrinterStatus 方法实现
   期望找到：实际检测打印机状态的逻辑
   ```

### 5.2 中优先级补充分析

1. **前端打印管理**
   ```
   建议搜索：nms4pos-ui 中 printer 相关页面
   期望找到：打印机配置、任务列表、重打功能
   ```

2. **PrintJobGenerator 详细逻辑**
   ```
   建议分析：1127 行代码的主要方法
   期望找到：generateKitchenJob, generateCustomerJob 等
   ```

### 5.3 低优先级补充分析

1. **WMS 打印场景**
   ```
   建议搜索：WMS_ST_BILL_* 样式类型使用场景
   期望找到：仓储单据打印集成
   ```

2. **报表打印**
   ```
   建议搜索：ShiftReport 等报表样式
   期望找到：交班单、日报表打印
   ```

---

## 6. 文档完整性检查

### 6.1 DA 文档检查

| 文档 | 状态 | 说明 |
|------|------|------|
| DA0-侦察报告 | ✅ | 分析范围、目标、方法 |
| DA1-业务切面分析 | ✅ | 业务背景、用例、流程 |
| DA2-概念字典 | ✅ | 术语定义 |
| DA3-关系分析 | ✅ | 实体关系、依赖关系 |
| DA4-规则分析 | ✅ | 业务规则、约束 |
| DA5-数据模型 | ✅ | 表结构、字段、枚举 |
| DA6-交互流程 | ✅ | 生命周期、调用序列 |
| DA7-实现映射 | ✅ | 代码位置、模块归属 |
| DA8-收敛分析 | ✅ | 关键发现、风险、建议 |

### 6.2 理解层文档检查

| 文档 | 状态 | 说明 |
|------|------|------|
| 00-十分钟读懂 | ✅ | 快速概览 |
| 01-业务全景 | ✅ | 详细业务说明 |
| 02-典型故事 | ✅ | 故事化场景 |
| 03-失败恢复 | ✅ | 故障处理 |
| 04-设计与实现 | ✅ | 技术架构 |
| 05-证据与未知 | ✅ | 证据清单 |
| G5验收 | 🔜 | 待完成 |

---

## 7. 后续探索方向

| 方向 | 说明 | 价值 |
|------|------|------|
| 前端打印管理 | 分析 nms4pos-ui 打印相关页面 | 理解完整用户体验 |
| 云打印 SDK | 分析 JBCloudPrinter 实现 | 理解云打印机制 |
| 报表打印 | 分析交班单、日报表生成 | 扩展打印场景 |
| 打印监控 | 补充监控告警设计 | 提高可运维性 |

---

**文档完整性：16/16 (DA) + 5/6 (理解层) + 0/1 (G5) = 71%**
