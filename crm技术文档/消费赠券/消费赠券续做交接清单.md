# 消费赠券续做交接清单

## 1. 目标

继续实现“消费赠券”方案：

- CRM 提供同步幂等发券/撤销接口。
- POS 门店维护本地 session/round/event 状态机。
- 接入结账、反结账、退款/重算流程。
- 保留 CRM 旧 MQ 消费赠券链路，不改旧行为。

## 2. 先读文档

优先阅读：

- `D:\mywork\techdoc\crm技术文档\消费赠券\消费赠券CRM与门店POS联动设计.md`

## 3. 当前已完成

### 3.1 文档

已新增：

- `D:\mywork\techdoc\crm技术文档\消费赠券\消费赠券CRM与门店POS联动设计.md`

### 3.2 POS 已完成

`D:\mywork\nms4pos` 已完成消费赠券底层骨架：

- `nms4cloud-pos2plugin-api`
  - 新增 `CrmConsumeCouponSourceEnum`
  - 新增 `CrmConsumeCouponSessionStatusEnum`
  - 新增 `CrmConsumeCouponRoundStatusEnum`
  - 新增 `CrmConsumeCouponEventTypeEnum`
  - 新增 `CrmConsumeCouponEventStatusEnum`
  - 新增 `CrmConsumeCouponGrantDTO`
  - 新增 `CrmConsumeCouponRevokeDTO`
  - 新增 `CrmConsumeCouponResultVO`
- `nms4cloud-pos2plugin-dal`
  - 新增 `CrmConsumeCouponSession`
  - 新增 `CrmConsumeCouponRound`
  - 新增 `CrmConsumeCouponEvent`
  - 新增对应 Mapper
- `sql/update`
  - 新增 `2026-5-15-crm-consume-coupon.sql`
- `nms4cloud-pos2plugin-biz`
  - `Nms4CloudCrmService` 已加：
    - `/crm_consume_coupon/grantSign`
    - `/crm_consume_coupon/revokeSign`

说明：POS 业务 service 和流程接入还没完成。

### 3.3 CRM 已完成到一半

`D:\mywork\nms4cloud` 已开始新增 CRM 幂等接口骨架：

- `nms4cloud-crm-api`
  - 新增 `CrmConsumeCouponOpStatusEnum`
  - 新增 `CrmConsumeCouponGrantDTO`
  - 新增 `CrmConsumeCouponRevokeDTO`
  - 新增 `CrmConsumeCouponResultVO`
- `nms4cloud-crm-dao`
  - 新增 `CrmConsumeCouponOpRecord`
  - 新增 Mapper / Service / ServiceImpl / xml
- `nms4cloud-crm-service`
  - 新增 `CrmConsumeCouponOpServicePlus`
- `nms4cloud-crm-app`
  - 新增 `CrmConsumeCouponOpController`
- `docs/sql/migration`
  - 新增 `V20260515__crm_consume_coupon_op_record.sql`

说明：CRM 代码还没完成编译验证，服务实现也需要继续检查导入、枚举、字段兼容和注释完整性。

## 4. 下次会话优先顺序

1. 先检查两个仓库 git status：
   - `D:\mywork\nms4cloud`
   - `D:\mywork\nms4pos`
2. 先编译/修正 CRM 新增接口：
   - 重点检查 `CrmConsumeCouponOpServicePlus`
   - 确认 DTO、entity、controller、service、SQL 一致
3. 再实现 POS `CrmConsumeCouponLocalService`
   - 参照 `CrmPointsEarnLocalService`
   - 但赠券规则不在 POS 算，POS 只落 session/round/event 并调 CRM
4. 再接入业务入口：
   - `DwdBillOpsServiceImpl` 正常结账
   - `DwdBillOpsServiceImpl` 反结账
   - `DwdBillOpsServiceImpl` 退款重算
   - `DwdBillOpsServiceImpl` 批量撤销/全单退款
   - `CashPayHandler` 快捷结账链路
5. 最后做聚焦编译验证

## 5. 关键设计约束

- 不实现统一底座，先只做消费赠券。
- CRM 只负责“幂等发券/幂等撤销”。
- POS 负责状态机和补偿。
- 首版不把 POS ActiveMQ 放进主链路。
- round 表承担轻量 outbox / work queue 角色。
- 保持详细中文代码注释。
- 不使用 Claude Code 评审。

## 6. 下次开新会话可直接发

```text
继续上次消费赠券实现。
先读：
D:\mywork\techdoc\crm技术文档\消费赠券\消费赠券CRM与门店POS联动设计.md
D:\mywork\techdoc\crm技术文档\消费赠券\消费赠券续做交接清单.md

然后检查这两个仓库的未提交改动并继续实现：
D:\mywork\nms4cloud
D:\mywork\nms4pos

要求：
1. 不要重做方案分析
2. 基于现有代码继续
3. 先完成 CRM 幂等发券/撤销接口并编译通过
4. 再完成 POS 本地状态机和结账/反结账/退款接入
5. 保持详细中文代码注释
```