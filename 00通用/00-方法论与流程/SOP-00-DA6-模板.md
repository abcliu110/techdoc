# DA6-交互流程 模板（独立文件 — 生成 DA6 文档时强制读取本文件）

> **依据**：SOP-00 §0 执行前置第 1/5/6 条。生成 DA6 文档前必须读取本文件，
> 并在 DA 文档开头输出"模板加载记录"（例：已读取 SOP-00-DA6-模板.md，门禁检查 N 项全部通过）。
> 本文件从 SOP-00 §9.5.7 拆出，**以本文件为准**（SOP-00 全文中的模板仅作历史参考）。

## 门禁检查（生成前必填，未全部通过 → 停止生成，先补齐）

- [ ] 是否覆盖了全部核心业务场景（至少 3 个）？
- [ ] 每个场景是否有参与者/前置条件/后置条件？
- [ ] 时序图是否覆盖正常流程 + 异常分支？
- [ ] API 清单是否每条都有证据？

**未全部通过 → 停止生成，先补齐**

## 模板正文

```markdown
# DA6 交互流程 — {分析对象}

## 核心交互场景
### 场景一：{场景名}
```
参与者: {角色1}、{角色2}、{系统}
前置条件: {条件}
后置条件: {结果}

流程:
┌──────┐    ┌──────┐    ┌──────┐    ┌──────┐
│{角色1}│    │{系统1}│    │{系统2}│    │{角色2}│
└──┬───┘    └──┬───┘    └──┬───┘    └──┬───┘
   │           │           │           │
   │ {动作}    │           │           │
   │──────────▶│           │           │
   │           │ {动作}    │           │
   │           │──────────▶│           │
   │           │           │ {动作}    │
   │           │           │──────────▶│
   │           │◀──────────│           │
   │◀──────────│           │           │
```

### 场景二：{场景名}
...

## 二、API 清单
| 方法 | 路径 | 请求 | 响应 | 证据 |
|---|---|---|---|---|

## 三、未知项（U-*）
| 编号 | 描述 | 影响 |
|---|---|---|
```

**全面性检查清单**：
- [ ] 是否覆盖了所有核心业务场景（至少 3 个以上，视系统复杂度而定）？
- [ ] 每个场景是否有参与者/前置条件/后置条件？
- [ ] 每个场景的时序图是否覆盖了正常流程 + 异常分支？
- [ ] 是否覆盖了 API 清单？每个 API 是否有证据？
- [ ] 时序图中的交互是否与 DA3 的 REL 关系一致？
- [ ] **叙事质量**：每个场景是否有"为什么这样设计"的解释（DEC 卡或文字说明）？异常分支是否有具体情境？


## 生成后必填

1. **模板加载记录**：DA 文档开头输出"模板加载记录"（读取了哪个独立模板文件、门禁检查结果），未输出视为未执行 SOP-00 §0 第 1 条。
2. **模板字段对照表**：DA 文档末尾输出"模板字段对照表"（模板要求字段 vs 实际输出，缺失必须补齐或登记 U-*），禁止虚假声称。
3. **独立验证**：全部 DA 完成后，由未参与生成的独立 agent 执行 V 验证（SOP-00 §0 第 5 条，禁止自检）。
