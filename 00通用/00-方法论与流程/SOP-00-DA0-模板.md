# DA0-侦察报告 模板（独立文件 — 生成 DA0 文档时强制读取本文件）

> **依据**：SOP-00 §0 执行前置第 1/5/6 条。生成 DA0 文档前必须读取本文件，
> 并在 DA 文档开头输出"模板加载记录"（例：已读取 SOP-00-DA0-模板.md，门禁检查 N 项全部通过）。
> 本文件从 SOP-00 §9.5.1 拆出，**以本文件为准**（SOP-00 全文中的模板仅作历史参考）。

## 门禁检查（生成前必填，未全部通过 → 停止生成，先补齐）

- [ ] 是否覆盖了全部 HTTP 入口（Controller）？
- [ ] 是否覆盖了消息监听（MQ/Event）、定时任务（Job/Cron）入口？
- [ ] 是否覆盖了核心表（数据 Schema）与配置项？
- [ ] 每个候选事实是否标注验证状态（推断/交叉验证/直接事实）？未验证的是否登记 U-*？

**未全部通过 → 停止生成，先补齐**

## 模板正文

```markdown
# DA0 侦察报告 — {分析对象}

## 版本信息
| 属性 | 值 |
|---|---|
| 侦察范围 | {仓库路径/模块} |
| 版本基线 | {分支/提交} |
| 侦察时间 | {日期} |

## 一、入口痕迹清单（R1 输出，候选线索，非业务结论）
### 1.1 Controller 入口
| 入口 | 路径 | 方法 | 证据 |
|---|---|---|---|
| {类名} | {HTTP 路径} | {方法} | E-SRC: {文件:行号} |

### 1.2 消息监听入口
| 入口 | 主题/队列 | 处理器 | 证据 |
|---|---|---|---|

### 1.3 定时任务入口
| 任务名 | 周期 | 执行逻辑 | 证据 |
|---|---|---|---|

### 1.4 数据痕迹
| 表名 | 核心字段 | 用途 | 证据 |
|---|---|---|---|

### 1.5 配置痕迹
| 配置项 | 值 | 用途 | 证据 |
|---|---|---|---|

## 二、候选事实清单（R2 输出）
| 候选事实 | 痕迹位置 | 推断链 | 置信度 | 验证状态 |
|---|---|---|---|---|
| {描述} | E-SRC: {文件:行号} | {推理过程} | 推断/交叉验证/直接事实 | 待验证/已验证 |

**全面性检查清单**：
- [ ] 是否覆盖了所有 HTTP 入口（Controller）？
- [ ] 是否覆盖了所有消息监听（MQ/Event）？
- [ ] 是否覆盖了所有定时任务（Job/Cron）？
- [ ] 是否覆盖了所有核心表（数据 Schema）？
- [ ] 是否覆盖了所有配置项？
- [ ] 每个候选事实是否标注了验证状态？未验证的是否登记了 U-*？

## 三、未知项（U-*）
| 编号 | 描述 | 影响 |
|---|---|---|

## 四、当前停止条件
- 继续侦察的预期信息增益：{高/中/低}
- 下一轮最小取证动作：{描述}
```


## 生成后必填

1. **模板加载记录**：DA 文档开头输出"模板加载记录"（读取了哪个独立模板文件、门禁检查结果），未输出视为未执行 SOP-00 §0 第 1 条。
2. **模板字段对照表**：DA 文档末尾输出"模板字段对照表"（模板要求字段 vs 实际输出，缺失必须补齐或登记 U-*），禁止虚假声称。
3. **独立验证**：全部 DA 完成后，由未参与生成的独立 agent 执行 V 验证（SOP-00 §0 第 5 条，禁止自检）。
