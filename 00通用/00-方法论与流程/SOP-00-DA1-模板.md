# DA1-业务切面分析 模板（独立文件 — 生成 DA1 文档时强制读取本文件）

> **依据**：SOP-00 §0 执行前置第 1/5/6 条。生成 DA1 文档前必须读取本文件，
> 并在 DA 文档开头输出"模板加载记录"（例：已读取 SOP-00-DA1-模板.md，门禁检查 N 项全部通过）。
> 本文件从 SOP-00 §9.5.2 拆出，**以本文件为准**（SOP-00 全文中的模板仅作历史参考）。

## 门禁检查（生成前必填，未全部通过 → 停止生成，先补齐）

- [ ] 业务域定位是否说明系统在更大系统中的位置与业务价值？
- [ ] 是否覆盖了全部核心角色（含异常处理角色）？
- [ ] 核心用例是否覆盖正常流程 + 异常流程？
- [ ] 每条业务规则是否有 E-* 证据或 U-* 标注？

**未全部通过 → 停止生成，先补齐**

## 模板正文

```markdown
# DA1 业务切面分析 — {分析对象}

## 一、业务域定位
### 1.1 系统位置
{ASCII 架构图：系统在更大系统中的位置}

### 1.2 业务价值
| 价值维度 | 说明 |
|---|---|

## 二、核心角色
| 角色 | 操作 | 接触系统 |
|---|---|---|

## 三、业务分类
### 3.1 {分类1}
| 业务项 | 触发时机 | 说明 |
|---|---|---|

### 3.2 {分类2}
...

## 四、核心用例
### UC-1：{用例名}
```
用例编号：UC-1
参与者：{角色}
前置条件：{条件}
基本流程：1. ... 2. ... 3. ...
后置条件：{结果}
异常处理：{处理方式}
```

### UC-2：{用例名}
...

## 五、业务规则汇总
| 规则编号 | 规则内容 | 约束级别 | 证据 |
|---|---|---|---|
| BR-01 | {规则} | MUST/SHOULD | E-SRC: {文件:行号} |

## 六、触发时机图
{ASCII 图：触发事件 → 业务动作 → 输出结果}

**全面性检查清单**：
- [ ] 是否覆盖了所有业务角色？（{角色1}、{角色2}、{角色3}等）
- [ ] 是否覆盖了所有业务分类？（每个分类下的业务项列举完整）
- [ ] 核心用例是否覆盖了正常流程 + 异常流程？
- [ ] 业务规则是否区分了 MUST/SHOULD/MAY？
- [ ] 触发时机图是否覆盖了所有业务事件？
- [ ] 每个断言是否有 E-* 证据或 U-* 标注？
```


## 生成后必填

1. **模板加载记录**：DA 文档开头输出"模板加载记录"（读取了哪个独立模板文件、门禁检查结果），未输出视为未执行 SOP-00 §0 第 1 条。
2. **模板字段对照表**：DA 文档末尾输出"模板字段对照表"（模板要求字段 vs 实际输出，缺失必须补齐或登记 U-*），禁止虚假声称。
3. **独立验证**：全部 DA 完成后，由未参与生成的独立 agent 执行 V 验证（SOP-00 §0 第 5 条，禁止自检）。
