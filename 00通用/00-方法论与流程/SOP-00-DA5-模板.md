# DA5-数据模型 模板（独立文件 — 生成 DA5 文档时强制读取本文件）

> **依据**：SOP-00 §0 执行前置第 1/5/6 条。生成 DA5 文档前必须读取本文件，
> 并在 DA 文档开头输出"模板加载记录"（例：已读取 SOP-00-DA5-模板.md，门禁检查 N 项全部通过）。
> 本文件从 SOP-00 §9.5.6 拆出，**以本文件为准**（SOP-00 全文中的模板仅作历史参考）。

## 门禁检查（生成前必填，未全部通过 → 停止生成，先补齐）

- [ ] 是否覆盖了全部核心表？
- [ ] 每个字段是否标注了业务含义与约束（主键/外键/唯一/非空）？
- [ ] 数据关系图是否与 DA3 的 REL 关系一致？
- [ ] 是否包含聚合根/值对象/领域服务/领域事件分析（不只表结构）？

**未全部通过 → 停止生成，先补齐**

## 模板正文

```markdown
# DA5 数据模型 — {分析对象}

## 核心表结构
### {表名1}
| 字段 | 类型 | 说明 | 业务含义 | 约束 | 证据 |
|---|---|---|---|---|---|

### {表名2}
...

## 关键索引
| 索引 | 字段 | 用途 |
|---|---|---|

## 数据关系
{ASCII 图：表之间的关联关系}
```

**全面性检查清单**：
- [ ] 是否覆盖了所有核心表？
- [ ] 每个表是否标注了业务含义（非裸字段名）？
- [ ] 每个字段是否标注了约束（主键/外键/唯一/非空）？
- [ ] 关键索引是否覆盖了查询场景？
- [ ] 数据关系图是否与 DA3 的 REL 关系一致？


## 生成后必填

1. **模板加载记录**：DA 文档开头输出"模板加载记录"（读取了哪个独立模板文件、门禁检查结果），未输出视为未执行 SOP-00 §0 第 1 条。
2. **模板字段对照表**：DA 文档末尾输出"模板字段对照表"（模板要求字段 vs 实际输出，缺失必须补齐或登记 U-*），禁止虚假声称。
3. **独立验证**：全部 DA 完成后，由未参与生成的独立 agent 执行 V 验证（SOP-00 §0 第 5 条，禁止自检）。
