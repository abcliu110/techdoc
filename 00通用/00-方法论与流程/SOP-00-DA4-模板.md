# DA4-规则分析 模板（独立文件 — 生成 DA4 文档时强制读取本文件）

> **依据**：SOP-00 §0 执行前置第 1/5/6 条。生成 DA4 文档前必须读取本文件，
> 并在 DA 文档开头输出"模板加载记录"（例：已读取 SOP-00-DA4-模板.md，门禁检查 N 项全部通过）。
> 本文件从 SOP-00 §9.5.5 拆出，**以本文件为准**（SOP-00 全文中的模板仅作历史参考）。

## 门禁检查（生成前必填，未全部通过 → 停止生成，先补齐）

- [ ] 是否覆盖了业务规则（BR）/重试规则（RR）/样式配置规则（SR）三层？
- [ ] 异常与恢复责任矩阵是否覆盖硬件/网络/协议/系统四层异常？
- [ ] 每个失败场景是否明确人工介入角色与动作？
- [ ] 每条规则是否有 E-* 证据或 U-* 标注？

**未全部通过 → 停止生成，先补齐**

## 模板正文

```markdown
# DA4 规则分析 — {分析对象}

## 一、业务规则
| 编号 | 规则描述 | 约束级别 | 实现位置 | 证据 |
|---|---|---|---|---|
| BR-01 | {规则} | MUST/SHOULD/MAY | {类:方法} | E-SRC: {文件:行号} |

## 二、重试/恢复规则
| 编号 | 规则描述 | 参数 | 证据 |
|---|---|---|---|
| RR-01 | {规则} | {参数值} | E-SRC: {文件:行号} |

## 三、样式/配置规则
| 编号 | 规则描述 | 约束级别 | 证据 |
|---|---|---|---|

## 四、技术约束
### 4.1 性能约束
| 约束项 | 目标值 | 说明 | 证据 |
|---|---|---|---|
### 4.2 可靠性约束
| 约束项 | 策略 | 说明 | 证据 |
|---|---|---|---|
### 4.3 兼容性约束
| 约束项 | 要求 | 证据 |
|---|---|---|

## 五、配置约束
{配置结构描述，按层级缩进}

## 六、异常与恢复责任矩阵

每个核心失败场景必须输出以下矩阵（至少覆盖：硬件/网络/协议/系统四层异常）：

| 失败场景 | 检测方式 | 自动恢复 | 人工介入角色 | 人工介入动作 | 升级路径 |
|----------|----------|----------|-------------|-------------|----------|
| {失败场景1} | {检测方式} | {自动恢复机制} | {角色} | {动作} | {升级路径} |
| {失败场景2} | {检测方式} | {自动恢复机制} | {角色} | {动作} | {升级路径} |
| {...} | {...} | {...} | {...} | {...} | {...} |

**覆盖要求**：
- 硬件层：{设备异常类型}
- 网络层：{网络异常类型}
- 协议层：{协议异常类型}
- 系统层：{系统异常类型}

## 七、未知项（U-*）
| 编号 | 描述 | 影响 |
|---|---|---|
```

**全面性检查清单**：
- [ ] 是否覆盖了所有业务规则（BR）？每个规则是否标注了 MUST/SHOULD/MAY？
- [ ] 是否覆盖了所有重试/恢复规则（RR）？参数值是否有证据？
- [ ] 是否覆盖了所有样式/配置规则（SR）？
- [ ] 是否覆盖了技术约束（性能/可靠性/兼容性）？
- [ ] 是否覆盖了配置约束（配置结构）？
- [ ] 恢复责任矩阵是否覆盖了所有声明的四层异常？
- [ ] 每个失败场景是否明确标注了人工介入角色和动作？
- [ ] 每个规则是否有 E-* 证据或 U-* 标注？
- [ ] 规则描述是否可去技术名？
- [ ] **叙事质量**：每个 MUST 规则是否写了"违反后果"（具体情境，不只是错误码枚举）？
```


## 生成后必填

1. **模板加载记录**：DA 文档开头输出"模板加载记录"（读取了哪个独立模板文件、门禁检查结果），未输出视为未执行 SOP-00 §0 第 1 条。
2. **模板字段对照表**：DA 文档末尾输出"模板字段对照表"（模板要求字段 vs 实际输出，缺失必须补齐或登记 U-*），禁止虚假声称。
3. **独立验证**：全部 DA 完成后，由未参与生成的独立 agent 执行 V 验证（SOP-00 §0 第 5 条，禁止自检）。
