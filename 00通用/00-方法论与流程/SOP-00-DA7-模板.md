# DA7-实现映射 模板（独立文件 — 生成 DA7 文档时强制读取本文件）

> **依据**：SOP-00 §0 执行前置第 1/5/6 条。生成 DA7 文档前必须读取本文件，
> 并在 DA 文档开头输出"模板加载记录"（例：已读取 SOP-00-DA7-模板.md，门禁检查 N 项全部通过）。
> 本文件从 SOP-00 §9.5.8 拆出，**以本文件为准**（SOP-00 全文中的模板仅作历史参考）。

## 门禁检查（生成前必填，未全部通过 → 停止生成，先补齐）

- [ ] 是否覆盖了全部核心业务概念到实现类的映射？
- [ ] 每个 DEC 卡是否包含全部 8 个字段？
- [ ] 是否包含关键代码示例或协议指令示例（不只类名表）？

**未全部通过 → 停止生成，先补齐**

## 模板正文

```markdown
# DA7 实现映射 — {分析对象}

## 核心类映射
| 业务概念 | 实现类 | 模块 | 关键方法 | 证据 |
|---|---|---|---|---|

## 配置映射
| 配置项 | 位置 | 默认值 | 说明 | 证据 |
|---|---|---|---|---|

## 设计决策记录（DEC 卡）
按 L1 §3.6 D1-D5 程序，对每个设计怪点产出 DEC 卡。

### DEC-001：{决策标题}
| 字段 | 内容 |
|---|---|
| 决策点 | {当时问的问题} |
| 当时约束 | {规模/框架/业务/团队上下文} |
| 可选方案 | {当时考虑的替代方案} |
| 选择与理由 | {选择的方案 + 理由 + 证据} |
| 成因分类 | 有意决策/历史遗留/偶然事故/兼容约束 |
| 当时合理性 | 合理/勉强/不合理 |
| 当前合理性 | 仍合理/已过期（约束变化） |
| 影响面 | 伤历史/只伤未来 |

### DEC-002：{决策标题}
...
```

**全面性检查清单**：
- [ ] 是否覆盖了所有核心业务概念到实现类的映射？
- [ ] 每个 DEC 卡是否包含全部 8 个字段（决策点/当时约束/可选方案/理由/成因/当时合理性/当前合理性/影响面）？
- [ ] 每个 DEC 卡是否有 E-* 证据？
- [ ] 是否覆盖了所有设计怪点（L1 §3.6 D1：不寻常/不直观/代价偏高的选择）？
- [ ] 配置映射是否覆盖了所有核心配置项？


## 生成后必填

1. **模板加载记录**：DA 文档开头输出"模板加载记录"（读取了哪个独立模板文件、门禁检查结果），未输出视为未执行 SOP-00 §0 第 1 条。
2. **模板字段对照表**：DA 文档末尾输出"模板字段对照表"（模板要求字段 vs 实际输出，缺失必须补齐或登记 U-*），禁止虚假声称。
3. **独立验证**：全部 DA 完成后，由未参与生成的独立 agent 执行 V 验证（SOP-00 §0 第 5 条，禁止自检）。
