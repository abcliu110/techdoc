# 历史混合 SOP 迁移记录

`mixed-sop/` 保存 2026-07-15 生成的第一版 309 份混合文档，仅用于审计和迁移追踪，不再是组件规范或生产 SOP 的权威来源。

已确认问题：

- 309 个组件的公开 API 均未冻结。
- 309 个组件的七道 Gate 均为未开始。
- 90 个组件使用通用“领域对象组合”核心动作。
- 84 个组件使用通用“结构化数据界面”核心动作。
- 风险清单存在与自身升级规则矛盾的条目。
- 旧生成器会覆盖全部文档和认证状态。
- 旧校验通过只证明数量、标题、元数据和链接完整，不证明语义正确。

这些文件不得作为 AI 编码输入。AI 只允许使用 `02-组件规范/` 中经批准的 `ImplementationReady` 规范。
