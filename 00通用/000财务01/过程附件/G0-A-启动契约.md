# G0-A 启动契约

> 模板来源：SOP-00 §5.1（G0-A）；独立模板加载：本阶段无独立 DA 模板文件，按 SOP-00 §5.1 字段填写。
> 生成日期：2026-08-07

## 契约字段

| 字段 | 内容 |
|---|---|
| 原始目标 | 用户原话："D:\mywork\techdoc\00通用\00-方法论与流程\SOP-00-业务系统分析SOP总览.md 执行这个sop, 对金蝶 财务领域进行分析， 产出文档放在 D:\mywork\techdoc\00通用\001财务"。预期认知：理解金蝶 K3Cloud 财务领域"如何运转、受何约束、如何失败、变更伤及何处" |
| 任务模式 | **认知重建**（接手/讲解/建立共同语言；只读，不实施方案） |
| 分析对象 | 金蝶云星空（K3Cloud）**财务领域（FIN）**：总账(GL)/应收(AR)/应付(AP)/现金(CB)/票据(CN) 及财务期间、核算、往来的业务机制 |
| 证据范围 | ① `C:\Users\16555\Downloads\01.K3Cloud源码\Business\FIN\`（执行域插件源码）② `C:\Users\16555\Downloads\01.K3Cloud源码\BOS\`（平台操作框架/元数据引擎/过账资源）③ 既有自产深度分析（`D:\mywork\techdoc\00通用\000财务\` 下本会话自产文档，作 E-DOC 候选）④ 元数据表结构约定（T_* 表，E-DAT 由 Schema/命名推断，具体表结构在服务端 DB，快照仅凭源码引用）⑤ 会计准则/行业通用知识（作背景，非项目证据，仅 E-DOC 通用层）。**无运行实例、无生产 DB 快照、无真实账套数据** |
| 输出与读者 | 输出：`D:\mywork\techdoc\00通用\001财务\`（理解层 00-05 + 过程附件 G0-A/G0-B/DA0-DA8）。读者：财务/研发双背景团队（可转述，不用读代码） |
| 动作边界 | 允许：只读源码分析与文档产出。禁止：修改/运行/部署 K3Cloud、连接生产库、写环境、发布任何变更。需授权：无 |
| 旧文档处理 | `001财务` 目录此前为空，本次全新产出；`000财务` 下**第三方生成的文档只读不触碰**（归 E-DOC 候选，数量以目录实况为准，本契约不再声明具体份数）；不覆盖任何既有文件 |
| 初始范围内/外 | 范围内：财务域的业务机制（期间/凭证/往来核销/对账/结账/报表口径）。范围外：供应链/制造的财务联动（仅引用）、财税具体税率计算、真实账套数据、性能实测 |
| 初始基线 | 仓库为反编译快照（非 git）。版本：对 `01.K3Cloud源码` 无提交标识可锁定；以"快照当前状态"为基线并在证据中用相对路径+符号定位。部署/数据版本未知，如实登记 U-G0-* |

## 停止条件（G0-A 层面）

- 最小：候选 DQ 与业务切片可由证据派生；每个主要结果族至少一个入口证据；已识别至少 1 业务写入口 + 1 结果状态 + 1 失败信号 + 1 跨边界链路。
- 认知重建无需用户预先提供领域概念；"无运行证据""业务裁决人未知"不以失败计，只限定证据层级并登记 U-*。

## 门禁结论

**G0-A：通过**（分析对象、证据范围、输出位置、只读边界明确；可启动只读侦察 DA0）。

## 模板字段对照表

| 模板字段 | 状态 |
|---|---|
| 原始目标/任务模式/分析对象/证据范围/输出读者/动作边界/旧文档处理/范围内外/初始基线/停止条件 | 全部填写，符合 §5.1 最低要求 |
