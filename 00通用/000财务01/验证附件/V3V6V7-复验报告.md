# V3/V6/V7 复验报告（第二轮 · 独立复核）

## 0. 范围与独立声明

### 0.1 复验范围

| 项 | 内容 |
|---|---|
| 复验对象 | 金蝶 K3Cloud 财务领域分析交付（`D:\mywork\techdoc\00通用\001财务\`，理解层 00-05 + 过程附件 G0-A/B、DA0~DA8 + README） |
| 基线 | 首轮报告 `V0-V7-独立验证报告.md`（裁定"条件通过"，列 P1-1~P1-5、P2/P3 必修修正项） |
| 本轮只复核 | 首轮裁定**不通过/条件**的门禁：**V3 / V6 / V7**；以及首轮 P1-1、P1-2、P1-3、P1-4（对抗闸一/二）、P2/P3 各必修分项。未泛查其他未涉项的符号与结论。 |
| 复核方式 | 全部只读：逐份读取修正后交付文档 + 首轮报告对照；源码复核两个关键符号方法签名（`AdjustPeriodServiceHelper`、`AcctgStencilServiceHelper`）及 4 个定位项 |
| 复核时间 | 2026-08-06 |
| 输出 | 本文（唯一写出文件）；未修改任何交付文档与源码 |

### 0.2 独立声明

- 本复验者**未参与**被验交付的生成与回退修正；未使用 DA 生成者任何私有上下文；未与修正者讨论。
- 结论按"默疑、逐格反查、以源码签名为准"作出；对"已修正/部分修正/未修正"逐项独立判定。
- 本轮只就**必修项是否到位**下结论，不重新裁定已被首轮放行的门禁。

---

## 1. 源码复核（先定语义之锚，再判修正是否到位）

### 1.1 `AdjustPeriodServiceHelper`（P1-1 的事实底座）
- **真实位置**：`Business\FIN\01.Core\Kingdee.K3.FIN.ServiceHelper\Kingdee.K3.FIN.ServiceHelper\AdjustPeriodServiceHelper.cs`
- **方法签名实测**（全部 19 个 public）：`GetBookMaxPeriod` / `GetBookPeriodList` / `GetBookMaxPeriodFromCalendar` / `GetBookYearListFromCalendar` / `GetBookStartPeriod` / `GetBookCurrentYear` / `IsNotExistAdjustBalanceData` / `IsExistAdjustBalanceData` / `IsExistAdjustPeriod` / `IsAdjustPeriod` / `GetBookAdjustPeriodList` / `GetOpenAdjustPeriodByBook`(x2) / `GetLastDateOfAcct` / `GetMutBookMaxPeriod` / `GetMutBookPeriodList` / `GetMutBookMaxPeriodFromCalendar` / `GetMutBookMinStartPeriod` / `GetAdjustPeriodBookId` / `GetAdjustPeriodBook`。
- **判定**：全部为 Get/Is 只读查询，**无任何 Close/UnClose/改期写方法**。修正后文档"查询门面/只读"表述与源码一致 **✅**。

### 1.2 `AcctgStencilServiceHelper`（P1-2 的事实底座）
- **真实位置**：同 ServiceHelper 目录。
- **方法签名实测**：`OutAcctgCheck` / `OutStockAcctg` / `SumSchedule` / `GetScheduleProgress` / `GetAnsyProgress` / `DeregisterTaskProgress`(x2) / `GetOutAcctgTable` / `SeriousErr` / `GetCost`；被 `...Business.DynamicForm.BusinessService\GetCostService.cs` 以 `AcctgStencilServiceHelper.GetCost(...)` 调用。
- **判定**：**出库核算/存货成本调度**，与凭证模板无关。修正后文档"出库核算/存货成本调度"表述与源码一致 **✅**。

### 1.3 定位项（P2/P3 相关）
- `VoucherGenerateService` / `BuildBusinessVoucher` / `ViewGlVoucher`：位于 `...Kingdee.K3.FIN.Business.DynamicForm.Operation\`（**非** ServiceHelper 目录）—— DA0/DA7 对这 3 类的模块标注偏粗（见 §2 残留）。
- `AgingAnalysisService`：位于 `BusinessPlugIn\Kingdee.K3.FIN.AP.App.Report\...`（另有 ServicePlugIn 同位）—— 05 残留"01.Core+AP"为错。
- `CashflowReport`：实际类名小写 f，位于 `...GL.Report.PlugIn\CashflowReport.cs`——DA0 已用小写 **✅**。
- `01.Core` 全目录无"结账"中文字符串，无含 `CloseAccount`/`UnClose`/`PeriodWrite` 的类文件，GL 结账写入口确未在 FIN 01.Core 定位（与 U-DA0-5 表述一致）。

---

## 2. 逐项核对表（必修项 × 状态 × 佐证）

| 项 | 必修内容 | 状态 | 佐证（修正后实测） | 残留 |
|---|---|---|---|---|
| **P1-1** | `AdjustPeriodServiceHelper` → "查询门面/结账写入口待补证 U-DA0-5"，S2 结论降级 | **⚠️ 部分修正** | ✅ DA0 §1.1 行+"⚠️证据实况"、U-DA0-5；DA4 SM-02 三行"机制推断；结账写入口 U-DA0-5"；DA4 §五 U 表 U-DA0-5 显式降级 BR-02/03/SM-02；DA6 场景二 ⚠️ 块；DA7 一表"查询门面+U-DA0-5"；05 DM-01 维⑦；00 证据边界；01 DEC-003；03 §四；G0-B 期间行；README | ❌ **DA4 BR-02 证据列仍为"E-SRC AdjustPeriod/结账服务"、BR-03 证据列仍为"E-SRC AdjustPeriodServiceHelper"**（未像 SM-02 改成"机制推断+U-DA0-5"）；❌ **DA6 §二 API 清单 AdjustPeriodServiceHelper 行仍为"结账/改期 / 结账/反结/改期 / 期间状态变更 / E-SRC"**——原语义错位原文残留 |
| **P1-2** | `AcctgStencilServiceHelper` → "记账模板实现未定位 U-DA2-1"，旧引用清除 | **✅ 已修正** | DA2 BC-06 行改"快照未定位独立实现…U-DA2-1"+显式"更正"段；DA3 OR-09 挂 U-DA2-1（无 AcctgStencil）；DA7 四配置映射"凭证模板/记账模板…U-DA2-1"；05 精简卡"记账模板…U-DA2-1"；全交付 grep：`AcctgStencil` 仅余 README v1.1 记录与 DA2 更正段（均为更正说明，非证据引用） | 无 |
| **P1-3** | E-SRC 引用路径 → `01.Core/Kingdee.K3.FIN.ServiceHelper/...`（+ Operation 类归 `DynamicForm.Operation`） | **⚠️ 部分修正** | ✅ DA0 §1.1 头已改真实 ServiceHelper 目录；DA7 模块列"FIN/01.Core ServiceHelper"（对 ServiceHelper 类正确） | ⚠️ DA6 §二 表头仍写"**FIN/01.Core BusinessService**"（旧路径残留）；DA0 §1.1 中 VoucherGenerateService/BuildBusinessVoucher/ViewGlVoucher 三行仍标"E-SRC: 同目录"（实际在 DynamicForm.Operation）；DA7 模块列对这 3 个 Operation 类标"ServiceHelper"亦偏 |
| **对抗闸一** | 每条 U 挂"威胁结论→临时降级" | **⚠️ 近似达标（主体 ✅）** | DA4 §五 U 表新增"威胁的结论（接受威胁并临时降级）"列（U-DA4-1、U-DA4-2、U-DA0-5）；DA8 §5.2 六行 U→结论→降级（U-DA0-5→S2/DQ-FACT-02/BR-02/03/SM-02；U-DA0-2→所有"实际会怎样"；U-DA0-1→DA5 表结构；U-DA4-2→DQ-RISK-01；U-DA2-1→BC-06/OR-09；U-DA6-1→过账/结账同事务叙述） | ⚠️ 严格"每 U"未满：U-DA0-3（元数据缺）、U-DA0-4（AR 服务面缺）、U-DA4-1 未进 DA8 全局表（仅 DA4 局部表与 §六未覆盖项承载）；核心威胁结论全覆盖 |
| **对抗闸二** | X-* 登记、竞争解释改结论状态、DQ-FACT-02 降级 | **✅ 已修正** | DA8 §5.1 补 X-DA8-1（AdjustPeriod 命名暗示写能力 vs 实测只读→降级 S2）、X-DA8-2（"已结禁改"承诺 vs 写入口缺→U-DA0-5）；§四 三条竞争解释均加"👉 影响"并落实到具体 DQ 状态变化；DQ-FACT-02 由"已闭环(机制级)"改为"保留→**条件成立(降级，非已闭环)**"；DA8 完成声明收敛为"条件成立（机制级…非阻断）+ 诚实声明" | 无 |
| **P2** | 规则编号单轨（DA1 BR-* ↔ DA4 BR-*） | **✅ 已修正** | DA1 §五改为 BR-01~05，与 DA4 BR-01/02/03/04/06 一一对齐；首轮 BR-S1-* 双轨编号已消失 | 无 |
| **P3** | `CashflowReport` 大小写 | **✅ 已修正** | DA0 §1.2 用 `CashflowReport`（小写 f）；源码 `CashflowReport.cs` 确认 | 无 |
| **P3** | `AgingAnalysisService` 位置（AP.App.Report） | **⚠️ 部分修正** | ✅ DA0 §1.2 注"(实际位于 AP.App.Report)"、DA7 行"FIN/AP.App.Report"、DA6"E-SRC(AP)"；源码 AP.App.Report 确认 | ❌ **05 DM-04 维⑦ 仍写"`...AgingAnalysisService`（E-SRC: FIN/01.Core+AP）"**——与源码/DA0/DA7 矛盾 |
| **P3** | G0-A/G0-B 文档数量口径 | **⚠️ 部分修正** | ✅ 数量矛盾解决：G0-A"数量以目录实况为准，不再声明具体份数"；实测 000财务=16 份 .md 与 G0-B/README"自产"兼容 | ⚠️ G0-A 内部仍并存"本会话自产文档（证据范围③）"与"第三方生成的文档（旧文档处理）"两口径 |

> 附：首轮另两处 P3（期间状态标签"已反结/重开"不统一、DA0 声称"破例说明已在主文档 §0"而 00 未收录）仍残留，但不在本轮必修清单内，仅登记。

---

## 3. V3 / V6 / V7 复验结论

### V3 规则、不变量与状态机（原：条件通过（调低））
- **原卡点**：SM-02/BR-02/03 依赖 AdjustPeriod（语义错位）、U 未挂结论。
- **复验**：SM-02 三行已改为"机制推断；结账写入口 U-DA0-5"，状态机证据不再把查询门面当写服务；U 挂结论与临时降级已执行（DA4 §五、DA8 §5.2）；DA1 BR-02/03 与 DA8 DQ-FACT-02 的降级在六处文档相互印证。
- **残留**：DA4 BR-02/BR-03 证据列仍标"E-SRC AdjustPeriod/结账服务"（与 SM-02 及 U-DA0-5 降级自相矛盾，属单元格未清）。
- **结论：通过**（1 处单元格残留需清理，不改变结论状态）。

### V6 实现与运行映射（原：不通过）
- **原卡点**：P1-1/P1-2 语义错位 ×2、P1-3 路径系统性错误、CashFlow 大小写、Aging 定位。
- **复验**：两语义错位在 DA7 映射行已重定位（结账/改期→查询门面+U-DA0-5；记账模板→U-DA2-1），源码签名已证实修正方向正确；DA0 §1.1 主路径已改真实 ServiceHelper 目录；CashflowReport 大小写已改。
- **残留（V6 域内）**：DA6 §二 API 清单 AdjustPeriodServiceHelper 行仍是"结账/改期…E-SRC"原语义错位原文，且表头仍"FIN/01.Core BusinessService"旧路径；05 DM-04 账龄位置仍"01.Core+AP"；DA0/DA7 对三个 Operation 类模块标注偏粗。这些残留不影响两个核心符号已按真实位置定位与 S2 已降级的事实，但 V6"证据位置可复现"门禁在 DA6 处仍有断点。
- **结论：条件通过**（主体修正到位；残留集中于 DA6 API 清单/表头与 05 一处，清理后可通过）。

### V7 综合收敛与交付（原：条件通过，最重项为对抗闸一/二违规）
- **原卡点**：无 X-*、竞争解释不改状态、U 未挂结论、DA8"完成=是"过度声明、编号双轨。
- **复验**：对抗闸一（§5.2 U→结论→降级）与对抗闸二（X-DA8-1/2、竞争解释"👉影响"、DQ-FACT-02 降为条件成立）均已执行；DA8 完成声明收敛为"条件成立（机制级）+缺口+继续路径"；规则编号单轨化。
- **残留**：仅一般性 P2（全交付 E-SRC 仍无文件:行号，本轮未列入必修，不构成阻断）；§5.2 未含 U-DA0-3/4、U-DA4-1。
- **结论：通过**（对抗闸违规已消除；一般性证据粒度 P2 维持原判不影响）。

---

## 4. 遗留 P0 / P1

- **无 P0**。
- **遗留 P1（轻度，属原 P1-1/P1-3 未完全传播）**：
  1. **DA4**：BR-02 证据列"E-SRC AdjustPeriod/结账服务"、BR-03 证据列"E-SRC AdjustPeriodServiceHelper"。**应回退 DA4**，将这两列改为"机制推断；结账写入口 U-DA0-5"（与 SM-02 一致）。
  2. **DA6**：§二 API 清单 AdjustPeriodServiceHelper 行"结账/改期…E-SRC"（原语义错位原文残留）、§二表头"FIN/01.Core BusinessService"（旧路径残留）。**应回退 DA6**：该行改"期间查询门面（只读）；结账写入口 U-DA0-5"，表头路径改"FIN/01.Core/Kingdee.K3.FIN.ServiceHelper"。
- 遗留 P3（不阻断）：05 DM-04 账龄位置仍"01.Core+AP"；G0-A"自产/第三方"描述并存；DA0 §1.1 对 Operation 类"同目录"标注；DA8 §5.2 未含 U-DA0-3/4、U-DA4-1；期间状态标签"已反结/重开"不统一；00 未收录 DA0 声称的破例说明。

---

## 5. 最终裁定

### 裁定：维持"条件通过"（可升级性已基本具备，待 DA4/DA6 两处残留清理后可升为"通过"）

**理由**：

1. **两处关键语义错误已被真实证据纠正**：源码实测证明 `AdjustPeriodServiceHelper` 确属只读查询门面、`AcctgStencilServiceHelper` 确属出库核算调度，修正方向与源码一致（本轮核心复核项通过）。
2. **对抗闸一/二已实质执行**：X-DA8-1/2 已登记、三条竞争解释均标注对结论状态的影响、U 表挂"威胁结论→临时降级"、DQ-FACT-02 由"已闭环"降为"条件成立"、DA8 完成声明收敛——v3.12 最重违规形态已消除，V7 因此可判通过。
3. **S2 结论已系统性降级**：DA0/DA1/DA4(SM-02)/DA6 场景二/DA7/05/00/03/G0-B/README 多处将结账机制标为"推断/条件成立/待补证 U-DA0-5"，首轮"把查询门面当结账写服务"的实质危害已解除，V3 证据关由"不过"转为可过。
4. **不可直接升"通过"的单一原因**：必修项 P1-1 未完全传播——**DA4 BR-02/BR-03 证据列与 DA6 §二 API 清单行**仍保留原语义错位表述（虽同文档 U 台账已兜底降级、不承担实质结论）。在"逐格复核、独立默认怀疑"的纪律下，必修项不齐即维持条件状态；此为轻量残留，非新一轮实质发现。

故：V3、V7 复验为**通过**，V6 复验为**条件通过**；聚合裁定维持**条件通过**，并给出明确的清理路径（回退 DA4、DA6 各两处单元格），完成后本轮复验即可升级为"通过"。

---

## 6. 局限自评

- 本轮只读复核了必修项所处文档与两个关键符号；未对其它 12 个 ServiceHelper/报表符号重新逐签核（首轮已核，非必修）。
- 未逐份重读 `000财务` 16 份 E-DOC 内容真实性（仅核目录数与口径）。
- U-DA0-5"GL 结账写入口未定位"仍为静态搜索结论："未找到"不等于快照中不存在（服务端/资源/命名差异可能）；本轮仅核实 `01.Core` 无"结账"字符串与 Close 类，方向支持该登记，最终仍待服务端实证。
- 未执行任何运行/数据库验证；未对每条 BR/INV/SM 做代码级判定。
- "已修正/部分修正/未修正"判据为本复验者独立设定并逐格取证；残留单元格都已在报告中给出文件与原文，可复核。