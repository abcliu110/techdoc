# G0-A 启动契约 — 金蝶 K3Cloud 财务域

| 项 | 内容 |
|---|---|
| 任务模式 | 认知重建（只读分析，不改码、不下实施指令） |
| 分析对象 | 金蝶 K3Cloud 财务域（FIN：凭证/过账、期间/结账、往来核销/对账、资金/票据、披露/多账簿） |
| 证据范围 | 工作区既有语料：`001财务`（E-SRC 方法签名、X-DA8-1、DEC-001~004）、`000财务`（账类抽象、进化提炼） |
| 并非证据 | K3Cloud 源码实体、运行环境、生产 Schema——本机不可访问，声明为 U |
| 只读边界 | 仅读取已有 md/模板；不读外部源码；不写任何生产侧文件 |
| 输出位置 | `D:\mywork\techdoc\00通用\000财务02`（新目录，独立于 001，不覆盖旧产物） |
| 禁止动作 | 补造源码/运行/Schema 证据；把"能力"写成"已发生"；先入为主套模式名 |
| 关键前置 | 归纳/进化格须按 DA8 模板"六"节填执行痕迹；验证须独立 agent 执行（禁止自检） |

**最小反证**：若本次任何关键结论无 `E-语料: <文件>` 或 `U-*` 支撑 → G0-A 不通过，回补证据或登记未知。
