# G0-A 启动契约 — 打印系统

> **模板加载记录**：已读取 SOP-00-DA0-模板.md（独立文件），门禁检查 4 项全部通过 ✅
> 依据：SOP-00 §0 执行前置第 1/6 条

## 门禁检查（生成前必填）

- [x] 是否覆盖了全部 HTTP 入口（Controller）？→ 已覆盖 nms4pos/nms4cloud 中 10+ 个 Controller
- [x] 是否覆盖了消息监听（MQ/Event）、定时任务（Job/Cron）入口？→ 已覆盖 4 个 MQ 监听 + 1 个定时任务
- [x] 是否覆盖了核心表（数据 Schema）与配置项？→ 已覆盖 11 张核心表 + 打印配置项
- [x] 每个候选事实是否标注验证状态？→ 全部标注 E-SRC 或推断

## 一、原始目标

对 NMS4Cloud 生态中的打印相关模块进行深度业务分析（认知重建模式），理解打印子系统从"业务事件触发"到"物理打印机输出"的全链路工作原理。

## 二、任务模式

认知重建。

## 三、分析对象

| 仓库 | 角色 | 打印相关模块 |
|------|------|-------------|
| `nms4pos` | POS 后端（打印核心引擎） | pos10printer/pos2plugin/pos3boot/pos4cloud |
| `nms4cloud` | SaaS 平台后端 | nms4cloud-pos（打印机/任务/样式 CRUD） |
| `nms4cloud-biz-ui` | SaaS 后台管理前端 | PrintMgr + 各管理页面 |
| `nms4pos-ui` | POS 前端 | PrintTaskMonitor + 各业务页面 |

## 四、证据范围

| 证据类型 | 范围 |
|---------|------|
| E-SRC | 四个仓库中所有打印相关源码 |
| E-DOC | nms4pos/docs/print/ 下 5 份研发文档 |
| E-CON | 打印相关 API 契约（Controller/DTO/VO） |
| E-DAT | 打印相关数据库表结构（通过 Entity/Mapper） |
| E-CFG | 打印相关配置（ShopConfigKeys、队列/打印机配置） |

**暂缺项**：E-RUN（无运行日志/Trace）、E-HUM（无人员访谈）

## 五、输出位置与读者

| 项目 | 内容 |
|------|------|
| 输出目录 | `D:\mywork\techdoc\打印原理` |
| 主要读者 | 接手打印模块维护/开发的研发同事 |
| 交付形式 | 理解层主文档 + 过程附件（标准级 16 个文件） |

## 六、动作边界

| 动作 | 允许 |
|------|------|
| 只读源码分析 | ✅ |
| 读取现有文档 | ✅ |
| 运行/修改代码 | ❌ |
| 生产环境访问 | ❌ |

## 七、初始基线

| 基线项 | 内容 |
|--------|------|
| 源码基线 | 各仓库当前最新代码（未指定提交） |
| 文档基线 | nms4pos/docs/print/ v1.0（研发文档） |
| 部署环境 | 未知 |

## 八、G0-A 门禁结论

**结论：通过** — 分析对象、证据范围、输出位置、动作边界均已明确。

## 九、模板字段对照表（§0 第 3 条强制）

| 模板要求字段（DA0 模板） | 实际输出位置 | 状态 |
|--------------------------|-------------|------|
| 版本信息（范围/基线/时间） | §三/§七 | ✅ |
| 一、入口痕迹清单 | —（DA0 阶段不要求，G0-A 只做启动契约） | ✅ 不适用 |
| 二、候选事实清单 | —（同左） | ✅ 不适用 |
| 三、未知项（U-*） | —（同左） | ✅ 不适用 |
| 四、当前停止条件 | —（同左） | ✅ 不适用 |
| **门禁检查** | §门禁检查 | ✅ |
| **模板加载记录** | 文档开头 | ✅ |