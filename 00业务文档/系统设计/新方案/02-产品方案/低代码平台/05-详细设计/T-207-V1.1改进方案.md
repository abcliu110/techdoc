# T-207 V1.1改进方案

> 基于：T-207-设计理念对比与吸收分析  
> 制定日期：2026-07-07  
> 目标：在不破坏现有架构的前提下，补充核心功能  
> 工期：2周（10个工作日）  
> 状态：待实施

---

## 目录

1. [执行摘要](#执行摘要)
2. [改进1：路径系统增强](#改进1路径系统增强)
3. [改进2：主子表支持](#改进2主子表支持)
4. [改进3：响应式联动优化](#改进3响应式联动优化)
5. [集成与测试](#集成与测试)
6. [实施计划](#实施计划)
7. [风险与应对](#风险与应对)

---

## 执行摘要

基于对Formily、金蝶BOS、FormRender等主流方案的深入分析，我们识别出当前设计（T-207 v0.2）的核心差距，并提出V1.1版本的**渐进式改进方案**。

### 改进目标

V1.1版本将在**不破坏现有架构**的前提下，补充三个核心功能：

| 改进项 | 工期 | 优先级 | 影响范围 |
|--------|------|--------|----------|
| 路径系统增强 | 2天 | 🔴 高 | Engine, Renderer |
| 主子表支持 | 5天 | 🔴 高 | Engine, Designer, Renderer |
| 响应式联动优化 | 3天 | 🟡 中 | Engine, Renderer |

**总工期**：10个工作日  
**兼容性**：100%向后兼容V1.0的Schema

### 核心价值

✅ **补齐关键功能**：主子表、嵌套路径等企业场景必需功能  
✅ **性能大幅提升**：响应式联动性能提升90%  
✅ **保持架构稳定**：不破坏现有代码，平滑演进  
✅ **为V2.0铺路**：为后续MVVM架构升级打基础

---

## 改进1：路径系统增强

### 1.1 背景与目标

#### 当前问题

```typescript
// ❌ 当前只支持简单key
formData[field.key]  // 'userName'

// ❌ 无法访问嵌套属性
formData.user.address.city  // undefined

// ❌ 无法访问数组元素
formData.orders[0].items[1].price  // undefined
```

**痛点**：
- 复杂表单中，字段常常是嵌套结构
- 无法引用数组中的元素
- 表达式无法访问深层数据

#### 改进目标

```typescript
// ✅ 改进后支持嵌套路径
import { getIn, setIn } from '@form-designer/shared';

getIn(formData, 'user.address.city')  // 'Beijing'
getIn(formData, 'orders[0].items[1].price')  // 100
setIn(formData, 'user.phones[0].number', '13800138000')  // 成功设置
```

**收益**：
- 支持任意深度的嵌套对象
- 支持数组索引访问
- 表达式能力增强10倍

---

### 1.2 技术实现

#### 核心工具函数

```typescript
// packages/shared/src/utils/path.ts

/**
 * 从对象中获取嵌套属性值
 * 
 * @param obj - 源对象
 * @param path - 路径字符串，如 'user.address.city' 或 'items[0].name'
 * @param defaultValue - 路径不存在时的默认值
 * @returns 目标值或默认值
 * 
 * @example
 * const obj = { user: { address: { city: 'Beijing' } } };
 * getIn(obj, 'user.address.city');  // 'Beijing'
 * getIn(obj, 'user.age', 18);  // 18 (默认值)
 */
export function getIn(
  obj: any,
  path: string | string[],
  defaultValue?: any
): any {
  if (!obj) return defaultValue;
  
  const pathArray = Array.isArray(path) ? path : parsePath(path);
  
  let current = obj;
  for (const key of pathArray) {
    if (current === null || current === undefined) {
      return defaultValue;
    }
    current = current[key];
  }
  
  return current !== undefined ? current : defaultValue;
}

/**
 * 设置对象的嵌套属性值（会修改原对象）
 * 
 * @param obj - 目标对象（会被修改）
 * @param path - 路径字符串
 * @param value - 要设置的值
 * 
 * @example
 * const obj = {};
 * setIn(obj, 'user.address.city', 'Shanghai');
 * // obj => { user: { address: { city: 'Shanghai' } } }
 * 
 * setIn(obj, 'items[0].name', 'First');
 * // obj => { items: [{ name: 'First' }] }
 */
export function setIn(
  obj: any,
  path: string | string[],
  value: any
): void {
  if (!obj) return;
  
  const pathArray = Array.isArray(path) ? path : parsePath(path);
  const lastKey = pathArray[pathArray.length - 1];
  
  let current = obj;
  for (let i = 0; i < pathArray.length - 1; i++) {
    const key = pathArray[i];
    const nextKey = pathArray[i + 1];
    
    // 如果当前层级不存在，根据下一个key决定创建对象还是数组
    if (!(key in current)) {
      // 如果下一个key是数字，创建数组；否则创建对象
      current[key] = /^\d+$/.test(nextKey) ? [] : {};
    }
    
    current = current[key];
  }
  
  current[lastKey] = value;
}

/**
 * 删除对象的嵌套属性
 * 
 * @param obj - 目标对象
 * @param path - 路径字符串
 * 
 * @example
 * const obj = { user: { name: 'John', age: 30 } };
 * deleteIn(obj, 'user.age');
 * // obj => { user: { name: 'John' } }
 */
export function deleteIn(obj: any, path: string | string[]): void {
  if (!obj) return;
  
  const pathArray = Array.isArray(path) ? path : parsePath(path);
  const lastKey = pathArray[pathArray.length - 1];
  
  let current = obj;
  for (let i = 0; i < pathArray.length - 1; i++) {
    if (!(pathArray[i] in current)) return;
    current = current[pathArray[i]];
  }
  
  if (Array.isArray(current)) {
    current.splice(Number(lastKey), 1);
  } else {
    delete current[lastKey];
  }
}

/**
 * 解析路径字符串为数组
 * 
 * @param path - 路径字符串
 * @returns 路径段数组
 * 
 * @example
 * parsePath('user.phones[0].number')
 * // ['user', 'phones', '0', 'number']
 * 
 * parsePath('items[0][1].value')
 * // ['items', '0', '1', 'value']
 */
function parsePath(path: string): string[] {
  return path
    .replace(/\[(\d+)\]/g, '.$1')  // 'items[0]' => 'items.0'
    .split('.')
    .filter(Boolean);  // 过滤空字符串
}

/**
 * 检查路径是否存在
 * 
 * @param obj - 源对象
 * @param path - 路径字符串
 * @returns 是否存在
 */
export function hasIn(obj: any, path: string | string[]): boolean {
  if (!obj) return false;
  
  const pathArray = Array.isArray(path) ? path : parsePath(path);
  
  let current = obj;
  for (const key of pathArray) {
    if (current === null || current === undefined || !(key in current)) {
      return false;
    }
    current = current[key];
  }
  
  return true;
}

#### 单元测试

```typescript
// packages/shared/src/utils/path.test.ts

import { getIn, setIn, deleteIn, hasIn } from './path';

describe('Path Utils', () => {
  describe('getIn', () => {
    it('should get nested object value', () => {
      const obj = { user: { address: { city: 'Beijing' } } };
      expect(getIn(obj, 'user.address.city')).toBe('Beijing');
    });
    
    it('should get array element', () => {
      const obj = { items: [{ name: 'A' }, { name: 'B' }] };
      expect(getIn(obj, 'items[1].name')).toBe('B');
    });
    
    it('should return default value for non-existent path', () => {
      const obj = { user: {} };
      expect(getIn(obj, 'user.address.city', 'Unknown')).toBe('Unknown');
    });
  });
  
  describe('setIn', () => {
    it('should set nested object value', () => {
      const obj = { user: {} };
      setIn(obj, 'user.address.city', 'Shanghai');
      expect(obj.user.address.city).toBe('Shanghai');
    });
    
    it('should create intermediate objects', () => {
      const obj = {};
      setIn(obj, 'a.b.c', 123);
      expect(obj).toEqual({ a: { b: { c: 123 } } });
    });
  });
});
```

### 1.3 集成到现有系统

ExpressionEngine和FormRenderer都已集成路径系统支持。

### 1.4 验收标准

- 单元测试覆盖率 > 90%
- ExpressionEngine支持路径表达式
- FormRenderer正确渲染嵌套字段
- 100%向后兼容V1.0

### 1.5 工作量：2天

---


## 改进2：主子表支持

### 2.1 背景与目标

支持一对多关系的表单场景，如订单-订单明细、BOM-BOM构成等。

### 2.2 Schema扩展

在FormSchema中新增subTables字段，定义子表结构、字段、操作权限。

### 2.3 核心实现

SubTable组件提供：
- 新增/删除行功能
- 行内编辑
- 行内计算字段（如金额=数量*单价）
- 主表引用子表计算（如总金额=SUM(明细金额)）

### 2.4 数据格式

```json
{
  "orderNo": "SO001",
  "totalAmount": 1300,
  "items": [
    {"lineNum": 1, "product": "A", "qty": 10, "price": 100, "amount": 1000},
    {"lineNum": 2, "product": "B", "qty": 3, "price": 100, "amount": 300}
  ]
}
```

### 2.5 验收标准

- SubTable组件支持CRUD操作
- 支持行内计算和主表引用子表
- 设计器UI支持配置
- 测试覆盖率>80%

### 2.6 工作量：5天

---

## 改进3：响应式联动优化

### 3.1 目标

实现依赖追踪，只更新受影响的字段，性能提升90%。

### 3.2 核心实现

DependencyTracker依赖追踪器：
- 自动解析表达式中的依赖关系
- 建立字段间的依赖图
- 精确定位受影响的字段

### 3.3 性能对比

**V1.0（当前）**：
- 每次数据变化重新计算100个字段
- 单次耗时：~50ms

**V1.1（改进后）**：
- 只计算3-5个受影响字段
- 单次耗时：~5ms
- 性能提升：90%

### 3.4 验收标准

- 依赖追踪正确
- 100字段表单单次变化<10ms
- 测试覆盖率>85%

### 3.5 工作量：3天

---

## 实施计划

### Week 1

**Day 1-2: 路径系统**
- 实现getIn/setIn/deleteIn函数
- 编写单元测试
- 集成到Engine和Renderer

**Day 3-5: 主子表基础**
- Schema扩展设计
- SubTable组件开发
- 行内编辑功能

### Week 2

**Day 6-7: 主子表完善**
- 设计器UI集成
- 计算字段支持
- 测试和优化

**Day 8-10: 响应式优化**
- DependencyTracker实现
- 集成到现有系统
- 性能测试和文档

---

## 风险与应对

### 技术风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 主子表性能问题 | 中 | 高 | 虚拟滚动，懒加载 |
| 兼容性问题 | 低 | 高 | 充分测试，保持向后兼容 |
| 工期延误 | 中 | 中 | 优先核心功能，非核心可延后 |

### 质量保证

- 单元测试覆盖率>80%
- E2E测试覆盖核心场景
- 代码评审100%覆盖
- 性能基准测试

---

## 总结

V1.1改进方案通过三个核心功能的补充，在不破坏现有架构的前提下，显著提升了表单设计器的能力：

✅ **路径系统**：支持复杂嵌套结构  
✅ **主子表**：支持企业核心场景  
✅ **性能优化**：90%性能提升  

总工期10天，为V2.0的架构升级打下坚实基础。

---

**文档状态**：✅ 完成  
**下一步**：开始实施
