# T-207 全新表单设计器：00-08 评审修复记录

> 状态：v0.1
> 日期：2026-07-09
> 范围：`00-08` 文档跨文档一致性评审与修复

---

## 1. 评审方式

已执行：

1. Claude Code 对 `08-测试验收与质量门禁` 单篇评审。
2. Claude Code 对 `00-02` 批次评审。
3. Claude Code 对 `07-08` 批次评审。
4. 本地扫描检查旧 schemaPath、旧 version/status、testId 残留和乱码。

说明：

`03-06` 全文批次和摘录批次评审多次超时无输出，因此本轮先修复已返回评审中的跨批次问题，并通过本地扫描核对。后续若需要，可继续采用更小粒度方式评审 `03`、`04`、`05`、`06` 单篇。

---

## 2. 已修复问题

| 编号 | 问题 | 修复位置 | 状态 |
|---|---|---|---|
| R-001 | `version` 字段混用 draft/published/archived 字符串 | `00-参考产品拆解`、`01-产品定位与范围` | 已修复 |
| R-002 | Analyzer 示例 schemaPath 使用旧 `components.EntryTable_entries.detailTables[0]` | `02-信息架构与工作台` | 已修复 |
| R-003 | P0 规则边界中"计算"含义模糊 | `01-产品定位与范围`、`02-信息架构与工作台` | 已修复 |
| R-004 | P0 EntryTable 明细与 P1 动态规则边界不清 | `02-信息架构与工作台` | 已修复 |
| R-005 | PageRoot 是否计入 100 个布局容器限制不清 | `01-产品定位与范围` | 已修复 |
| R-006 | EventBus 缺少 `schema:updated` / `schema:published` | `02-信息架构与工作台` | 已修复 |
| R-007 | 权限 Tab 与规则 Tab 的显隐边界不清 | `02-信息架构与工作台` | 已修复 |
| R-008 | 大纲树误把设计器 TopCommandBar 放入运行态页面结构 | `02-信息架构与工作台` | 已修复 |
| R-009 | SplitPane 两子区约束只在拖放说明里出现 | `02-信息架构与工作台` | 已修复 |
| R-010 | P0 ReferencePicker 数据源边界不清 | `02-信息架构与工作台` | 已修复 |
| R-011 | `designer-selection-{nodeId}` 未在 07/08 收口 | `07-Schema与运行时架构`、`08-测试验收与质量门禁` | 已修复 |
| R-012 | 表格列头 testId 示例旧格式 | `05-强表格与分录设计` | 已修复 |
| R-013 | `basePublishedVersion` 用于 stale draft 但未定义 | `00`、`07`、`08` | 已修复 |
| R-014 | `schemaStatus` / `status` 命名冲突 | `07` 改为统一使用 PageSchema 顶层 `status`，业务状态使用 `bizState` | 已修复 |
| R-015 | RuntimeRenderer 三模式未说明权限状态差异 | `07-Schema与运行时架构` | 已修复 |
| R-016 | PerformanceGate 阈值未闭合 | `07`、`08` | 已修复 |
| R-017 | 发布证据包缺少版本基准 | `08-测试验收与质量门禁` | 已修复 |

---

## 3. 本地扫描结果

已确认以下旧格式未在当前核心文档中残留：

| 检查项 | 结果 |
|---|---|
| `"version": "draft"` | 未命中 |
| `"version": "published"` | 未命中 |
| `components.EntryTable_entries.detailTables` | 未命中 |
| `table-column-col_material` | 未命中 |
| `designer-selection` | 已在 07/08 定义 |
| `basePublishedVersion` | 已在 00/07/08 定义 |
| `schema:updated` | 已在 01/02 定义 |

`schemaStatus` 仅在旧评审报告中出现，不作为当前 PageSchema 规范。

---

## 4. 暂不继续事项

用户说明 `AI研发流程规范.md` 仍在修改，因此在完成 `00-08` 评审修复后，后续不继续按旧流程直接推进。

下一步必须：

```text
重新读取最新版 AI研发流程规范.md
  -> 确认阶段产物和顺序
  -> 再决定是否补正式需求规格/功能规格/原型图/交互图
```

---

## 5. 剩余风险

| 风险 | 说明 | 处理建议 |
|---|---|---|
| `03-06` 未完成稳定 Claude 批次评审 | 长文档评审多次超时 | 后续按单篇或更小摘录继续评审 |
| 旧评审报告可能与新规范冲突 | 99 目录中存在历史评审报告 | 后续引用时以当前 `00-08` 正文为准 |
| AI 流程规范仍在修改 | 阶段产物顺序可能变化 | 等流程稳定后重新读取 |

---

## 6. 当前判定

`00-08` 已完成主要跨文档问题修复，达到进入"重新读取最新版研发流程规范并决定后续路径"的条件。
